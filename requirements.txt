python-telegram-bot[job-queue]==20.3
requests==2.31.0
python-dotenv==1.0.0
pdfplumber==0.10.3
httpx==0.24.1
asyncpg==0.30.0
pytz
numpy==1.26.4

# ── CPU-only PyTorch ──────────────────────────
# ВАЖНО: torch устанавливается ПЕРВЫМ в Dockerfile отдельной командой
# с --index-url https://download.pytorch.org/whl/cpu
# Здесь он НЕ указан, чтобы pip не перетянул CUDA-версию (~800MB).
# Если вы ставите локально: pip install torch==2.4.0+cpu --index-url https://download.pytorch.org/whl/cpu

sentence-transformers==2.7.0
transformers==4.40.0