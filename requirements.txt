python-telegram-bot[job-queue]==20.3
requests==2.31.0
python-dotenv==1.0.0
pdfplumber==0.10.3
httpx==0.24.1
asyncpg==0.30.0
pytz
numpy==1.26.4

# ── ONNX Runtime replaces PyTorch ─────────────
# PyTorch: ~200MB RAM at runtime
# ONNX Runtime: ~30MB RAM at runtime
# Same model, same vectors, fraction of the memory.
onnxruntime==1.17.0
optimum[onnxruntime]==1.17.1
transformers==4.40.0
sentence-transformers==2.7.0